import { Placeholder } from '../../../../components/Placeholder';

// Reference: Beacon 09 Match Lobby.dc.html — built in task 8.
// Must render correctly as a cold-start deep-link target (match-notify T-10m push).
export default function MatchLobby() {
  return <Placeholder title="Match Lobby" reference="Beacon 09 Match Lobby.dc.html" />;
}
