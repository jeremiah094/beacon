// Shared between the league create/edit form and the Settings screen's
// "default region" picker, so there's one source of truth for the list.
export const REGIONS = ['Ireland-wide', 'Leinster', 'Munster', 'Connacht / Ulster'];
